apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-crt
spec:
  dnsNames:
  - 'grafana-monitoring.apps.bm.0.0.0.0.sslip.io'
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned
  secretName: grafana-crt