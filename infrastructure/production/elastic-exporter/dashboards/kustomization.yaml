namespace: monitoring

generatorOptions:
  labels:
    grafana-liveinfra-dashboard: default
  disableNameSuffixHash: true

configMapGenerator:
  - name: elasticsearch-cluster-dashboards
    files:
      - elastic-exporter.json
  - name: elasticsearch-cluster-dashboards
    files:
      - elastic-exporter-overview.json

