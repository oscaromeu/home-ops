apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pyroscope-certificate
spec:
  dnsNames:
  - 'pyroscope.prod.0.0.0.0.sslip.io'
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned
  secretName: pyroscope-crt
