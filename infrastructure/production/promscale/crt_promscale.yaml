apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: promscale-certificate
spec:
  dnsNames:
  - 'promscale.prod.0.0.0.0.sslip.io'
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned
  secretName: promscale-crt
