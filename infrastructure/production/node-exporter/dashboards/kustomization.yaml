namespace: monitoring

generatorOptions:
  labels:
    grafana-liveinfra-dashboard: default
  disableNameSuffixHash: true

configMapGenerator:
  - name: node-exporter
    files:
      - node-exporter.json