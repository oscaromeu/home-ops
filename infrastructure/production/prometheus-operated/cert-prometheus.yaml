apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard
  namespace: monitoring
spec:
  dnsNames:
  - 'prometheus.prod.192.168.1.45.sslip.io'
  issuerRef:
    kind: ClusterIssuer
    name: self-signed
  secretName: dashboard-crt