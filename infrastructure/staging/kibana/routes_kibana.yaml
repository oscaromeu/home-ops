---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: kibana-int-http
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`kibana.staging.0.0.0.0.sslip.io`)
      middlewares:
        - name: redirect-permanent
          namespace: monitoring
      services:
        - name: kibana
          port: 5601