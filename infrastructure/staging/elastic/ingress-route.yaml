---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: es-elastic-stack
spec:
  entryPoints:
    - websecure
  routes:
    - match: "Host(`elastic.staging.0.0.0.0.sslip.io`)"
      kind: Rule
      services:
        - name: es-elasticsearch
          port: 9200
          scheme: https
#      middlewares:
#        - name: es-middleware-chain
#    - match: "Host(`elastic.staging.0.0.0.0.sslip.io`) && ((PathPrefix(`/_search`) && Method(`POST`)) || Method(`GET`))"
#      kind: Rule
#      services:
#        - name: es-elasticsearch-read
#          port: 9200
#          scheme: https
#      middlewares:
#        - name: es-middleware-chain
#    - match: "Host(`elastic.staging.0.0.0.0.sslip.io`) && Method(`DELETE`,`POST`,`PUT`)"
#      kind: Rule
#      services:
#        - name: es-elasticsearch-write
#          port: 9200
#          scheme: https
#      middlewares:
#        - name: es-middleware-chain
    - match: "Host(`kibana.staging.0.0.0.0.sslip.io`)"
      kind: Rule
      services:
        - name: es-kb-http
          port: 5601
          scheme: https
#      middlewares:
#        - name: es-middleware-chain
#    - match: "Host(`apm.staging.0.0.0.0.sslip.io`)"
#      kind: Rule
#      services:
#        - name: es-apm-http
#          port: 8200
#          scheme: https
#      middlewares:
#        - name: es-ip-whitelist
#        - name: es-circuit-breaker
#        - name: es-apm-ratelimit
  tls:
    secretName: es-certs
    domains:
      - sans:
          - "elastic.staging.0.0.0.0.sslip.io"
          - "kibana.staging.0.0.0.0.sslip.io"
          - "apm.staging.0.0.0.0.sslip.io"
