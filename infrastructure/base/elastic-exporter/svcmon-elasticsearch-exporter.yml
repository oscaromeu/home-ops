---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-elasticsearch-exporter-svcmon
  namespace: monitoring
  labels:
    app: prometheus-elasticsearch-exporter
spec:
  selector:
    matchLabels:
      app: prometheus-elasticsearch-exporter
  endpoints:
  - port: http
    #interval: 15s