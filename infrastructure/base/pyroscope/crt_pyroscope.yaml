apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pyroscope-certificate
  namespace: cert-manager
spec:
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned
  secretName: pyroscope-crt