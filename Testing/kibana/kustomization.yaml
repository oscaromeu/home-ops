apiVersion: kustomize.config.k8s.io/v1beta1
namespace: monitoring
kind: Kustomization
resources:
  - kibana.yaml


# TODO: Needs some investigation not overriding default helm chart values
#apiVersion: kustomize.config.k8s.io/v1beta1
#namespace: monitoring
#kind: Kustomization
#resources:
#  - release.yaml 
#configMapGenerator:
#  - name: values-kibana
#    files:
#      - values.yaml=./config/values-kibana.yaml
#    namespace: monitoring
#configurations:
#  - kustomizeconfig.yaml
