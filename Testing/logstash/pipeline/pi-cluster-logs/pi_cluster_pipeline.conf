input {
    redis {
        host => "redis-master"
        data_type => "list"
        key => "k3s"
        ssl => true
        port => "6379"
    }
}

filter {
}


output {
  elasticsearch {
      hosts => ["http://elastic-prod-master-headless:9200"]
      index => "k3s"
  }
}