apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-ingest
  namespace: monitoring
  labels:
    component: elasticsearch
    role: ingest
spec:
  selector:
    component: elasticsearch
    role: ingest
  ports:
    - port: 9200
      name: rest
    - port: 9300
      name: inter-node
#type: LoadBalancer
