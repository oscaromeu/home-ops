---
apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSet
metadata:
  name: apps-flux-system
  namespace: flux-system
spec:
  inputs:
  - namespace: flux-system
    apps:
    - app: flux-operator
      wait: true
    - app: flux-webhook
      dependsOn:
      - name: flux-operator
