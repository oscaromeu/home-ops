alertmanager:
  route:
    main:
      enabled: true
      hostnames:
        - alertmanager.${SECRET_DOMAIN}
      parentRefs:
        - name: envoy-internal
          namespace: network
  alertmanagerSpec:
    replicas: 1
    alertmanagerConfiguration:
      name: alertmanager
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: ${STORAGE_CLASS}
          resources:
            requests:
              storage: 1Gi
