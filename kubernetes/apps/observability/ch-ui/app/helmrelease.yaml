---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ch-ui
spec:
  chartRef:
    kind: OCIRepository
    name: ch-ui
  values:
    controllers:
      ch-ui:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: ghcr.io/caioricciuti/ch-ui
              tag: v2.11.1@sha256:5df8f456db79d89fd648f28a58faeed2e9022ad560bc69cfe8b5cca1ec133bd2
            env:
              CLICKHOUSE_URL: http://clickhouse-clickhouse-headless.databases.svc.cluster.local:8123
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 25m
              limits:
                memory: 512Mi
    service:
      app:
        ports:
          http:
            port: 3488
    persistence:
      data:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: openebs-hostpath
        globalMounts:
          - path: /app/data
    route:
      app:
        hostnames: ["ch-ui.${SECRET_DOMAIN}"]
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: 3488
