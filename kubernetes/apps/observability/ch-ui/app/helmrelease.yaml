---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ch-ui
spec:
  chartRef:
    kind: OCIRepository
    name: ch-ui
  values:
    controllers:
      ch-ui:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: ghcr.io/caioricciuti/ch-ui
              tag: v2.5.1@sha256:019c7299985d4ee0503df324d17268a4b891bf2f1538d9687918806e7acbf316
            env:
              CLICKHOUSE_URL: http://clickhouse-clickhouse-headless.databases.svc.cluster.local:8123
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 25m
              limits:
                memory: 512Mi
    service:
      app:
        ports:
          http:
            port: 3488
    persistence:
      data:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: openebs-hostpath
        globalMounts:
          - path: /app/data
    route:
      app:
        hostnames: ["ch-ui.${SECRET_DOMAIN}"]
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: 3488
