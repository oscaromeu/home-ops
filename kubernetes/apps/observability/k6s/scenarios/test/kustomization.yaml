---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./test.yaml
  - ./snapshot.yaml

configMapGenerator:
  - name: test
    files:
      - test.js=./test.js
  - name: test-config
    files:
      - test.yaml=./test.yaml
generatorOptions:
  disableNameSuffixHash: false