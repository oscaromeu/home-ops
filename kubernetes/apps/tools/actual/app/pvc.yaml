---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual
  namespace: tools
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-path
