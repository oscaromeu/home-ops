---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: minio-tunnelbinding
  namespace: default
subjects:
  - name: minio
    spec:
      fqdn: minio.oscaromeu.io
tunnelRef:
  kind: ClusterTunnel
  name: cluster-tunnel
