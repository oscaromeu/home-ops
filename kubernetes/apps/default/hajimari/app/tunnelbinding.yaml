---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: hajimari-binding
  namespace: default
subjects:
  - name: hajimari
    spec:
      fqdn: "hajimari.${SECRET_DOMAIN}"
tunnelRef:
  kind: ClusterTunnel
  name: cluster-tunnel
