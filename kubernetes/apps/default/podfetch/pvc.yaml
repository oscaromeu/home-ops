---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: podfetch-db
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}
