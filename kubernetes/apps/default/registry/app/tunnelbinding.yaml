---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: docker-registry-tunnelbinding
  namespace: default
subjects:
  - name: docker-registry
    spec:
      fqdn: "registry.${SECRET_DOMAIN}"
tunnelRef:
  kind: ClusterTunnel
  name: cluster-tunnel
