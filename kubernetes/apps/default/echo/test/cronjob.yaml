---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: echo-test
  labels:
    app.kubernetes.io/name: echo-test
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 300
      ttlSecondsAfterFinished: 3600
      template:
        metadata:
          labels:
            app.kubernetes.io/name: echo-test
        spec:
          restartPolicy: Never
          initContainers:
            - name: load-tests
              image: ghcr.io/oscaromeu/playwright-runner:rolling@sha256:59571ab67851ba1af526d224b8064f347768613d56c28807651947c0ed19d47d
              command: ["sh", "-c", "cp -rL /cm/* /tests/ && ls -la /tests"]
              volumeMounts:
                - name: cm
                  mountPath: /cm
                - name: tests
                  mountPath: /tests
          containers:
            - name: playwright
              image: ghcr.io/oscaromeu/playwright-runner:rolling@sha256:59571ab67851ba1af526d224b8064f347768613d56c28807651947c0ed19d47d
              imagePullPolicy: IfNotPresent
              env:
                - name: PW_BASE_URL
                  value: https://echo.${SECRET_DOMAIN}
                - name: PROBE_NAME
                  value: echo
                - name: PROBE_ENV
                  value: homelab
                - name: CLICKHOUSE_URL
                  value: http://clickhouse-clickhouse-headless.databases.svc.cluster.local:8123
                - name: CLICKHOUSE_DATABASE
                  value: e2e
                - name: CLICKHOUSE_USER
                  value: default
              volumeMounts:
                - name: tests
                  mountPath: /tests
              resources:
                requests:
                  cpu: 250m
                  memory: 1Gi
                limits:
                  memory: 2Gi
          volumes:
            - name: cm
              configMap:
                name: echo-test
            - name: tests
              emptyDir: {}
