---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: escalidraw-tunnelbinding
  namespace: default
subjects:
  - name: excalidraw
    spec:
      fqdn: "draw.${PUBLIC_DOMAIN}"
tunnelRef:
  kind: ClusterTunnel
  name: cluster-tunnel
