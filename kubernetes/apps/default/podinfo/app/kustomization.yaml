apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../base/default/podinfo

configMapGenerator:
  - name: podinfo-configmap
    files:
      - podinfo.conf=./resources/podinfo.conf
generatorOptions:
  disableNameSuffixHash: true