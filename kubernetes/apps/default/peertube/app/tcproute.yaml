---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: peertube-rtmp
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: rtmp
  rules:
    - backendRefs:
        - name: peertube
          port: 1935
