---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/common-4.4.0/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app peertube
spec:
  chartRef:
    kind: OCIRepository
    name: outline
  values:
    controllers:
      peertube:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.4.0@sha256:5086f94abc783f1147d7c2a32c01db00ab594820026e4f6a82ac2af3dbde7fc7
            envFrom: &envFrom
              - secretRef:
                  name: postgres-${postgres_cluster_version}-pguser-peertube
        containers:
          app:
            image:
              repository: chocobozzz/peertube
              tag: production-bookworm@sha256:011b0ec43d921bd60a3aa01fc7995e42cf15275925f16bee6f17a6c896ca5060
            env:
              NODE_ENV: production
              PEERTUBE_WEBSERVER_HOSTNAME: "{{ .Release.Name }}.${SECRET_DOMAIN}"
              PEERTUBE_WEBSERVER_PORT: "443"
              PEERTUBE_WEBSERVER_HTTPS: "true"
              PEERTUBE_TRUST_PROXY: '["loopback", "uniquelocal"]'
              PEERTUBE_SMTP_ACTIVE: "false"
              PEERTUBE_SIGNUP_ENABLED: "false"
              PEERTUBE_LIVE_ENABLED: "true"
              PEERTUBE_LIVE_ALLOW_REPLAY: "true"
              PEERTUBE_LIVE_LATENCY_SETTING_ENABLED: "true"
              PEERTUBE_LIVE_RTMP_ENABLED: "true"
              PEERTUBE_LIVE_RTMP_PORT: "1935"
            envFrom: *envFrom
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v1/config
                    port: &port 9000
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 5
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
            resources:
              requests:
                cpu: 200m
                memory: 512Mi
              limits:
                memory: 2Gi
    defaultPodOptions:
      securityContext:
        seccompProfile: { type: RuntimeDefault }
    service:
      app:
        controller: peertube
        ports:
          http:
            port: *port
          rtmp:
            port: 1935
            protocol: TCP
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
    persistence:
      config:
        existingClaim: peertube
        globalMounts:
          - path: /config
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
      videos:
        type: nfs
        server: ${NAS_SERVER_IP}
        path: ${NAS_SERVER_MEDIA_PATH}
        advancedMounts:
          peertube:
            app:
              - path: /downloads
                subPath: Library/Peertube
              - path: /data
                subPath: Library/Peertube/data