apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

configMapGenerator:

  - name: kubernetes-overview
    files:
      - kubernetes-overview.json
    options:
      labels:
        grafana_dashboard: kubernetes-overview
      disableNameSuffixHash: true

