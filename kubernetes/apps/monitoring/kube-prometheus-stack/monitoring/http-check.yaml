apiVersion: canaries.flanksource.com/v1
kind: Canary
metadata:
  name: prometheus-http-check
spec:
  interval: 30
  http:
    - name: http pass response 200 status code
      endpoint: https://prometheus.${SECRET_DOMAIN}
      thresholdMillis: 3000
      responseCodes: [201, 200, 301]
