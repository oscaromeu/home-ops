---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:

  - name: nginx
    files:
      - ./nginx.json
    options:
      labels:
        home_ops: nginx
      disableNameSuffixHash: true
