apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging

configMapGenerator:

  - name: vector
    files:
      - ./vector.json
    options:
      labels:
        home_ops: vector
      disableNameSuffixHash: true

