apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging

configMapGenerator:

  - name: vector-1
    files:
      - ./vector_1.json
    options:
      labels:
        home_ops: vector
      disableNameSuffixHash: true

  - name: vector-2
    files:
      - ./vector_2.json
    options:
      labels:
        home_ops: vector
      disableNameSuffixHash: true

  - name: vector-3
    files:
      - ./vector_3.json
    options:
      labels:
        home_ops: vector
      disableNameSuffixHash: true

  - name: vector-4
    files:
      - ./vector_4.json
    options:
      labels:
        home_ops: vector
      disableNameSuffixHash: true

  - name: vector-5
    files:
      - ./vector_5.json
    options:
      labels:
        home_ops: vector
      disableNameSuffixHash: true
