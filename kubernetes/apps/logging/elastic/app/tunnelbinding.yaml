---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: kibana-tunnel
  namespace: logging
subjects:
  - name: elk-kb-http

tunnelRef:
  kind: ClusterTunnel
  name: cluster-tunnel
