---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: elastic-tunnel
  namespace: logging
subjects:
  - name: elastic
    spec:
      fqdn: elastic.devfu.io
tunnelRef:
  kind: ClusterTunnel
  name: cluster-tunnel
