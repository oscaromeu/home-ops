---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: logging

generatorOptions:
  labels:
    grafana-homeops-datasource: default
  disableNameSuffixHash: true

secretGenerator:
  - name: grafana-datasource-loki
    files:
      - loki.yaml
