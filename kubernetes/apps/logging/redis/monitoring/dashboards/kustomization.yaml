apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging

configMapGenerator:

  - name: redis
    files:
      - redis.json
    options:
      labels:
        grafana_dashboard: redis
      disableNameSuffixHash: true
