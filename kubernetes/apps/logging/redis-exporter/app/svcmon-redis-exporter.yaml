apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-exporter
  labels:
    app: redis-exporter
spec:
  namespaceSelector:
    matchNames:
      - logging
  selector:
    matchLabels:
      app: redis-exporter
  endpoints:
    - port: redis-metrics
      interval: 60s
