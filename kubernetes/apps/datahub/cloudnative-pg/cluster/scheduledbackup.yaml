---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres
  namespace: datahub
spec:
  schedule: "@weekly"
  immediate: true
  backupOwnerReference: self
  target: primary
  cluster:
    name: postgres
