---
apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: postgres-pguser-memo
spec:
  externalSecretName: postgres-pguser-memo-template
  namespaceSelectors:
    - matchLabels:
        kubernetes.io/metadata.name: default
  refreshTime: 1h
  externalSecretSpec:
    refreshInterval: 1h
    secretStoreRef:
      kind: ClusterSecretStore
      name: onepassword-connect
    target:
      name: postgres-pguser-memo
      creationPolicy: Owner
    data:
      - secretKey: uri
        remoteRef:
          key: postgres-pguser-memo
          property: uri
---
apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: postgres-test-restore-pguser-grafana
spec:
  externalSecretName: postgres-test-restore-pguser-grafana-template
  namespaceSelectors:
    - matchLabels:
        kubernetes.io/metadata.name: observability
  refreshTime: 1h
  externalSecretSpec:
    refreshInterval: 1h
    secretStoreRef:
      kind: ClusterSecretStore
      name: onepassword-connect
    target:
      name: postgres-test-restore-pguser-grafana
      creationPolicy: Owner
    data:
        # Database
      - secretKey: GF_DATABASE_NAME
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: dbname
      - secretKey: GF_DATABASE_HOST
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: host
      - secretKey: GF_DATABASE_PASSWORD
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: password
      - secretKey: GF_DATABASE_USER
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: user
        # Database Init
      - secretKey: INIT_POSTGRES_DBNAME
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: dbname
      - secretKey: INIT_POSTGRES_HOST
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: host
      - secretKey: INIT_POSTGRES_PASS
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: password
      - secretKey: INIT_POSTGRES_SUPER_PASS
        remoteRef:
          key: postgres-test-restore-pguser-postgres
          property: password
      - secretKey: INIT_POSTGRES_USER
        remoteRef:
          key: postgres-test-restore-pguser-grafana
          property: user
#---
#apiVersion: external-secrets.io/v1
#kind: ClusterExternalSecret
#metadata:
#  name: postgres-pguser-vikunja
#spec:
#  externalSecretName: postgres-pguser-vikunja-template
#  namespaceSelectors:
#    - matchLabels:
#        kubernetes.io/metadata.name: default
#  refreshTime: 1h
#  externalSecretSpec:
#    refreshInterval: 1h
#    secretStoreRef:
#      kind: ClusterSecretStore
#      name: onepassword-connect
#    target:
#      name: postgres-pguser-vikunja
#      creationPolicy: Owner
#    data:
#        # Database
#      - secretKey: VIKUNJA_DATABASE_DATABASE
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: dbname
#      - secretKey: VIKUNJA_DATABASE_HOST
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: host
#      - secretKey: VIKUNJA_DATABASE_PASSWORD
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: password
#      - secretKey: VIKUNJA_DATABASE_USER
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: user
#        # Database Init
#      - secretKey: INIT_POSTGRES_DBNAME
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: dbname
#      - secretKey: INIT_POSTGRES_HOST
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: host
#      - secretKey: INIT_POSTGRES_PASS
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: password
#      - secretKey: INIT_POSTGRES_SUPER_PASS
#        remoteRef:
#          key: postgres-pguser-postgres
#          property: password
#      - secretKey: INIT_POSTGRES_USER
#        remoteRef:
#          key: postgres-pguser-vikunja
#          property: user
---
apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: postgres-pguser-atuin
spec:
  externalSecretName: postgres-pguser-atuin-template
  namespaceSelectors:
    - matchLabels:
        kubernetes.io/metadata.name: default
  refreshTime: 1h
  externalSecretSpec:
    refreshInterval: 1h
    secretStoreRef:
      kind: ClusterSecretStore
      name: onepassword-connect
    target:
      name: postgres-pguser-atuin
      creationPolicy: Owner
    data:
        # Database
      - secretKey: ATUIN_DB_URI
        remoteRef:
          key: postgres-pguser-atuin
          property: uri
        # Database Init
      - secretKey: INIT_POSTGRES_DBNAME
        remoteRef:
          key: postgres-pguser-atuin
          property: dbname
      - secretKey: INIT_POSTGRES_HOST
        remoteRef:
          key: postgres-pguser-atuin
          property: host
      - secretKey: INIT_POSTGRES_PASS
        remoteRef:
          key: postgres-pguser-atuin
          property: password
      - secretKey: INIT_POSTGRES_SUPER_PASS
        remoteRef:
          key: postgres-pguser-postgres
          property: password
      - secretKey: INIT_POSTGRES_USER
        remoteRef:
          key: postgres-pguser-atuin
          property: user