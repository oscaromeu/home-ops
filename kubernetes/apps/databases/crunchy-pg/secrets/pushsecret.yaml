apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-pguser-memo
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: pgbouncer-uri
        remoteKey: postgres-pguser-memo
      secretKey: pgbouncer-uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-pguser-memo
  template:
    data:
      pgbouncer-uri: '{{ index . "pgbouncer-uri" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
