---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-${postgres_cluster_version}-pguser-memo
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-${postgres_cluster_version}-pguser-memo
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-${postgres_cluster_version}-pguser-memo
  template:
    data:
      uri: '{{ index . "uri" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-test-restore-pguser-grafana
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: uri
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: user
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-test-restore-pguser-grafana
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-test-restore-pguser-postgres
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-test-restore-pguser-postgres
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      superpassword: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-${postgres_cluster_version}-pguser-vikunja
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-${postgres_cluster_version}-pguser-vikunja
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-${postgres_cluster_version}-pguser-vikunja
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-${postgres_cluster_version}-pguser-vikunja
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-${postgres_cluster_version}-pguser-vikunja
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-${postgres_cluster_version}-pguser-vikunja
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-${postgres_cluster_version}-pguser-vikunja
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-${postgres_cluster_version}-pguser-vikunja
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-${postgres_cluster_version}-pguser-postgres
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-${postgres_cluster_version}-pguser-postgres
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-${postgres_cluster_version}-pguser-atuin
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-${postgres_cluster_version}-pguser-atuin
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-${postgres_cluster_version}-pguser-atuin
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-${postgres_cluster_version}-pguser-atuin
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-${postgres_cluster_version}-pguser-atuin
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-${postgres_cluster_version}-pguser-atuin
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-${postgres_cluster_version}-pguser-atuin
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-${postgres_cluster_version}-pguser-atuin
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-${postgres_cluster_version}-pguser-authentik
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-${postgres_cluster_version}-pguser-authentik
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-${postgres_cluster_version}-pguser-authentik
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-${postgres_cluster_version}-pguser-authentik
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-${postgres_cluster_version}-pguser-authentik
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-${postgres_cluster_version}-pguser-authentik
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-${postgres_cluster_version}-pguser-authentik
      secretKey: uri
  - conversionStrategy: None
    match:
      remoteRef:
        property: superpassword
        remoteKey: postgres-${postgres_cluster_version}-pguser-postgres
      secretKey: password
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword
  selector:
    secret:
      name: postgres-${postgres_cluster_version}-pguser-authentik
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
      superpass:
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace