---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-pguser-memo
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-pguser-memo
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-pguser-memo
  template:
    data:
      uri: '{{ index . "uri" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-test-restore-pguser-grafana
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: uri
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-test-restore-pguser-grafana
      secretKey: user
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-test-restore-pguser-grafana
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-test-restore-pguser-postgres
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-test-restore-pguser-postgres
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-test-restore-pguser-postgres
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      superpassword: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-pguser-vikunja
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-pguser-vikunja
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-pguser-vikunja
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-pguser-vikunja
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-pguser-vikunja
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-pguser-vikunja
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-pguser-vikunja
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-pguser-vikunja
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-pguser-postgres
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-pguser-postgres
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-pguser-postgres
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-pguser-postgres
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-pguser-postgres
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-pguser-postgres
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-pguser-postgres
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-pguser-postgres
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgres-pguser-atuin
  namespace: databases
spec:
  data:
  - conversionStrategy: None
    match:
      remoteRef:
        property: dbname
        remoteKey: postgres-pguser-atuin
      secretKey: dbname
  - conversionStrategy: None
    match:
      remoteRef:
        property: host
        remoteKey: postgres-pguser-atuin
      secretKey: host
  - conversionStrategy: None
    match:
      remoteRef:
        property: password
        remoteKey: postgres-pguser-atuin
      secretKey: password
  - conversionStrategy: None
    match:
      remoteRef:
        property: port
        remoteKey: postgres-pguser-atuin
      secretKey: port
  - conversionStrategy: None
    match:
      remoteRef:
        property: user
        remoteKey: postgres-pguser-atuin
      secretKey: user
  - conversionStrategy: None
    match:
      remoteRef:
        property: uri
        remoteKey: postgres-pguser-atuin
      secretKey: uri
  deletionPolicy: Delete
  refreshInterval: 5m
  secretStoreRefs:
  - kind: ClusterSecretStore
    name: onepassword-connect
  selector:
    secret:
      name: postgres-pguser-atuin
  template:
    data:
      dbname: '{{ index . "dbname" | toString }}'
      host: '{{ index . "host" | toString }}'
      password: '{{ index . "password" | toString }}'
      port: '{{ index . "port" | toString }}'
      uri: '{{ index . "uri" | toString }}'
      user: '{{ index . "user" | toString }}'
    engineVersion: v2
    mergePolicy: Replace
  updatePolicy: Replace