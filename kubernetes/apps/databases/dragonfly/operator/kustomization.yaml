---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Old versions on purpose: starting point to rehearse the
  # v1.1.7 -> v1.6.1 operator upgrade procedure.
  - https://raw.githubusercontent.com/dragonflydb/dragonfly-operator/v1.1.2/manifests/crd.yaml
  - ./rbac.yaml
  - ./ocirepository.yaml
  - ./helmrelease.yaml
