---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kafka-operator
spec:
  chart:
    spec:
      chart: strimzi-kafka-operator
      version: 0.49.0
      sourceRef:
        kind: HelmRepository
        name: strimzi
  valuesFrom:
    - kind: ConfigMap
      name: kafka-operator-helm-values