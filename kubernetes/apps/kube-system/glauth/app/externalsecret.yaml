---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: glauth
  namespace: kube-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: glauth-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        server.toml: |
          debug = true
          [ldap]
              enabled = true
              listen = "0.0.0.0:389"
          [ldaps]
              enabled = false
          [api]
              enabled = true
              tls = false
              listen = "0.0.0.0:5555"
          [backend]
              datastore = "config"
              baseDN = "dc=home,dc=arpa"
        groups.toml: |
          [[groups]]
              name = "svcaccts"
              gidnumber = 6500
          [[groups]]
              name = "admins"
              gidnumber = 6501
          [[groups]]
              name = "people"
              gidnumber = 6502
        # Create passbcrypt:
        #   https://gchq.github.io/CyberChef/#recipe=Bcrypt(12)To_Hex('None',0)
        # or
        #   htpasswd -bnBC 10 "" YOUR_PASSWORD | tr -d ':\n' | od -A n -t x1 | sed 's/ *//g' | tr -d '\n'
        users.toml: |
          [[users]]
              name = "{{ .GLAUTH_SEARCH_USERNAME }}"
              mail = "{{ .GLAUTH_OSCAR_EMAIL }}"
              uidnumber = 5000
              primarygroup = 6500
              passbcrypt = "{{ .GLAUTH_SEARCH_PASSWORD }}"
              [[users.capabilities]]
                  action = "search"
                  object = "*"
          [[users]]
              name = "{{ .GLAUTH_OSCAR_USERNAME }}"
              mail = "{{ .GLAUTH_OSCAR_EMAIL }}"
              givenname = "{{ .GLAUTH_OSCAR_GIVENNAME }}"
              sn = "{{ .GLAUTH_OSCAR_SURNAME }}"
              uidnumber = 5001
              primarygroup = 6501
              othergroups = [ 6502 ]
              passbcrypt = "{{ .GLAUTH_OSCAR_PASSWORD }}"
  dataFrom:
    - find:
        path: GLAUTH
