apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: json-fusion-exporter
  namespace: dev
resources:
  - ./externalsecret.yaml
  - ./deployment.yaml

configMapGenerator:
  - files:
      - ./config/config.yaml
    name: json-fusion-exporter
