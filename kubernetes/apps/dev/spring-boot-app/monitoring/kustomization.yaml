apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

configMapGenerator:

  - name: fastapi-app
    files:
      - ./dashboards/fastapi-app.json
    options:
      labels:
        home_ops: fastapi-app
      disableNameSuffixHash: true
