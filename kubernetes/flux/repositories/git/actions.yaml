---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: home-kubernetes
  namespace: flux-system
spec:
  interval: 30m
  ref:
    branch: main
  url: "ssh://git@github.com/oscaromeu/flanks-api-actions.git"
  secretRef:
    name: cluster-secrets-git
  ignore: |
    # exclude all
    /*
    # include kubernetes directory
    !/kubernetes
