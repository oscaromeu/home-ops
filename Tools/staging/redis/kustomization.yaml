apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: redis
  namespace: monitoring

resources:
  - ./secrets/secret-redis-ca.yaml
  - ./secrets/secret-redis-crt.yaml
  - ./secrets/secret-redis-pem.yaml
  - dc_redis.yaml
  - svc_redis-port.yaml
#  - route_redis-port.yaml

transformers:
  - labelTransformer.yaml
