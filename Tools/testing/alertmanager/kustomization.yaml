apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deploy_alertmanager.yaml
  - svc_alertmanager.yaml

configMapGenerator:
- name: alertmanager-config
  files:
    - alertmanager.yml
