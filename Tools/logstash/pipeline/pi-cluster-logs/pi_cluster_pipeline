input {

  redis {
      host => "redis-master"
      type => "redis-input"
      data_type => "list"
      key => "k3s"
      ssl => false
      port => 6379
  }

}

filter {
}


output {
  elasticsearch {
      hosts => ["http://elastic-prod-master-headless:9200"]
      index => "k3s"
  }
}