apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: redis
  namespace: monitoring

resources:
  - dc_redis.yaml
  - secret-redis-ca_sealed.yaml
  - secret-redis-crt_sealed.yaml
  - secret-redis-pem_sealed.yaml
  - svc_redis-port.yaml
#  - route_redis-port.yaml

transformers:
  - labelTransformer.yaml
