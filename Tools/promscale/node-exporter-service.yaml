apiVersion: v1
kind: Service
metadata:
  labels:
    service: node-exporter
  name: node_exporter
  namespace: monitoring
spec:
  ports:
    - name: "9100"
      port: 9100
      targetPort: 9100
  selector:
    service: node-exporter
status:
  loadBalancer: {}
