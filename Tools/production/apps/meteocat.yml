apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: meteocat
  namespace: monitoring
spec:
  schedule: "00 */1 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: meteocat-influx
            image: osokaru/meteocat:0.1.1-2-g635c1c7
          restartPolicy: OnFailure
