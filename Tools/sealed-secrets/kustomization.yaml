apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sealed-secrets
  namespace: kube-system
  labels:
    app: "sealed-secrets"
resources:
  #- namespace.yaml
  - controller.yaml
